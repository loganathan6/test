#include<stdio.h>
void main()
{
int a[15];
int v,i,m;
printf("enter the value");
scanf("%d",&v);
printf("enter the array");
for(i=0;i<v;i++);
{
scanf("%d",&a[i])
}
max=max[0]
for(i=0;i,v;i++)
{
if(max a[i])
max=a[i];
pritf("enter the maxmium value");
}
}
